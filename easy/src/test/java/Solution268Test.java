import java.util.Arrays;
import java.util.stream.Collectors;

public class Solution268Test {
    // TODO ADD TESTS
    private void set() {
        String[] a= new String[]{};
        Arrays.stream(a).collect(Collectors.toSet());
    }
}